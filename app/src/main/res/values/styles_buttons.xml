<resources>

    <!-- Base application theme. -->


    <style name="Button" parent="TextAppearance.AppCompat.Button">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/colorAccent</item>
        <item name = "android:background">@color/colorAccent</item>
    </style>

    <style name="Button.Fill.Accent.Blue" parent="Widget.AppCompat.Button">
        <!-- Customize your theme here. -->
        <item name="android:background">@drawable/btn_accent_fill_color</item>
        <item name="android:textSize">@dimen/textViewMedium</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">@dimen/paddingSmall</item>

    </style>

    <style name="Button.Border.Blue" parent="TextAppearance.MaterialComponents.Button">
        <!-- Customize your theme here. -->
        <item name="android:background">@drawable/btn_border_blue</item>
        <item name="android:textSize">@dimen/textViewMedium</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">@dimen/paddingSmall</item>
    </style>

</resources>
